// Copyright 2022 The Go Authors. All rights reserved.
// Use of this source code is governed by a BSD-style
// license that can be found in the LICENSE file.

//go:build go1.23

package a

import (
	"fmt"
	"os"
)

type A struct {
	b int
}

func singleAssignment() {
	if 1 == 1 {
	}

	panic("I should survive")
}

func noOtherStmtsInBlock() {
}

func partOfMultiAssignment() {
	_, err := os.Open("file") // want `declared and not used: f`
	panic(err)
}

func sideEffects(cBool chan bool, cInt chan int) {
	<-c            // want `declared and not used: b`
	fmt.Sprint("") // want `declared and not used: s`
	A{             // want `declared and not used: a`
		b: func() int {
			return 1
		}(),
	}
	A{<-cInt}          // want `declared and not used: c`
	fInt() + <-cInt    // want `declared and not used: d`
	fBool() && <-cBool // want `declared and not used: e`
	map[int]int{       // want `declared and not used: f`
		fInt(): <-cInt,
	}
	[]int{<-cInt}       // want `declared and not used: g`
	func(s string) {}() // want `declared and not used: i`
}

func commentAbove() {
	// v is a variable
}

func fBool() bool {
	return true
}

func fInt() int {
	return 1
}
